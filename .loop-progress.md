## Run 164 / 500 — severity day/week matrices + top-categorias

- **Feature**: new `lib/stats/severity-daily-matrix.ts` +
  `/api/estatisticas/severidade-por-dia.json` — day × severity level
  (1..5) matrix for the last N days (UTC), gap-filled. Complements
  the existing monthly variant for short-window heatmaps where month
  buckets are too lumpy.
- **Feature**: new `lib/stats/severity-weekly-matrix.ts` +
  `/api/estatisticas/severidade-por-semana.json` — ISO-Monday
  (UTC) week × severity matrix. Completes the severity-matrix
  resolution set: {por-dia, por-semana, por-mes}.
- **Feature**: `/api/top-categorias.json` — category leaderboard by
  weighted severity score (severity × exp(-ageDays/365)), primary
  tags only to keep signal clean. Closes the leaderboard family:
  {ranking, top-partidos, top-estados, top-categorias}. Two-step
  fetch (pivot rows → category metadata by id) to dodge the
  PostgREST `.or()`+`!inner` silent-drop pattern.
- **Bug fix**: `lib/stats/daily-buckets.ts::buildDailyStatusSeries`
  dereferenced `r.statement_date.slice(0,10)` without a null-guard.
  Added the now-standard `?. / if (!k) continue` — every daily
  endpoint benefits in one edit.
- **Cost opt**: the two new severity-matrix endpoints and their
  future scope variants will share a single helper each (daily /
  weekly) — same pattern that cut ~40 LOC per route on the monthly
  family. Leaderboard endpoint caps SWR at 86400 (vs 3600 on
  others) because the weighted ranking moves slowly; cheaper cache
  window for no freshness cost.

## Run 163 / 500 — severidade-por-mes scope variants + weekly null-guard

- **Feature**: new shared helper
  `lib/stats/severity-monthly-matrix.ts::buildSeverityMonthlyMatrix`.
  Encapsulates the month × severity-level (1..5) tally, null-guards
  `statement_date`, gap-fills against a UTC-anchored window, skips
  non-1..5 severity values. Replaces ~40 LOC of inline loops.
- **Feature (refactor)**:
  `/api/estatisticas/severidade-por-mes.json` now calls the helper —
  drops inline tally + loop, keeps request/ETag/cache shape intact.
- **Feature**: `/api/partidos/:slug/severidade-por-mes.json` —
  scoped severity matrix for a single party via
  `politicians!inner(party)` + `.eq('politicians.party', party)`.
- **Feature**: `/api/estados/:uf/severidade-por-mes.json` — same
  matrix scoped to a UF (`politicians!inner(state)`).
- **Feature**: `/api/categorias/:slug/severidade-por-mes.json` —
  scoped via `statement_categories!inner(statements)` pivot with
  `is_primary=true` (keeps signal clean, matches other categoria-
  scoped endpoints).
- **Feature**: `/api/fontes/:domain/severidade-por-mes.json` —
  same matrix for statements citing a source domain, using the
  existing `sourceUrlOrFilter`.
- **Feature**: `/api/politico/:slug/severidade-por-mes.json` —
  rounds out the full scope-variant set: {global, partido, estado,
  categoria, fonte, politico} all share one helper.
- **Bug fix**: `app/api/politico/[slug]/por-semana.json/route.ts`
  dereferenced `r.statement_date.slice(0,10)` without a null-guard
  — would throw if a row ever came back with a null date. Added
  the same `?. / if (!raw) continue` pattern used elsewhere.
- **Bug fix**: `lib/stats/weekly-buckets.ts::buildWeeklyStatusSeries`
  had the same latent null-deref. Fix once in the shared helper;
  every weekly-bucket endpoint benefits automatically.
- **Cost opt**: helper-first refactor means the 6 severity-matrix
  endpoints share one tally + gap-fill + null-guard. Future
  DB-side aggregate swap (materialized view or RPC) lands in a
  single file. No new DB queries added; all endpoints keep the
  minimal 2-column select (`statement_date, severity_score`).

## Run 162 / 500 — matrix helper + por-categoria/por-fonte

- **Feature**: new shared helper `lib/stats/group-matrix.ts`
  (`buildGroupMatrix`, `monthKeysUtc`). Generic grouping → top-N →
  gap-filled month×key matrix builder. Replaces ~30 LOC of
  ad-hoc loops per endpoint with a typed, reusable path.
- **Feature**: `/api/estatisticas/por-categoria.json` — month ×
  category matrix for the top N categories by primary-tag volume.
  Inner-joins `statement_categories` → `statements` with
  `is_primary=true`. Returns category label + color alongside the
  series so frontends can style without a second lookup.
- **Feature**: `/api/estatisticas/por-fonte.json` — month × source
  domain matrix. Uses `extractDomain` to normalize
  `primary_source_url`, emits classification (imprensa/oficial/
  video/social/outro) per domain. Completes the matrix-axis set:
  {partido, estado, categoria, fonte}.
- **Feature (refactor)**: `/api/estatisticas/por-partido.json` and
  `/api/estatisticas/por-estado.json` now use `buildGroupMatrix` —
  ~60 LOC of duplicated grouping / gap-fill / top-N code deleted
  across the two files.
- **Bug fix**: prior inline loops filtered month keys only
  implicitly (whatever `monthKeys.includes`/`Map.has` happened to
  catch). Helper uses an explicit `monthSet` built from the same
  UTC anchor used for rendering — guarantees the dateFn output and
  the gap-fill rows share exactly one source of truth.
- **Cost opt**: all four matrix endpoints now share caps (50k rows
  for non-pivot, 60k for pivot) + identical ETag shape. A future
  swap to a DB-side aggregate materialized view lands in a single
  helper rewrite.

## Run 161 / 500 — global matrices trio + null-date guard sweep

- **Feature**: `/api/estatisticas/severidade-por-mes.json` — month
  × severity level (1..5) matrix for the last N months (UTC), gap-
  filled. `?meses=3..36`. Lets a dashboard render severity
  heatmaps from one request.
- **Feature**: `/api/estatisticas/por-partido.json` — month × party
  matrix for top N parties by volume in the window. Single fetch
  → grouped in memory → monthly gap-fill per party. `?meses=3..24`,
  `?limite=1..30`.
- **Feature**: `/api/estatisticas/por-estado.json` — mirror for the
  top N UFs. Same shape as por-partido so frontend chart code can
  swap the axis.
- **Bug fix**: hunted down four `statement_date.slice(0, 7)` sites
  without null guards — `politico/sparkline.json`,
  `politico/monthly.json`, `categorias/stats.json`,
  `verify-ratio.json`. A single `null` date row would have crashed
  the whole response; now they drop silently.
- **Cost opt**: all three new matrices emit content-keyed weak
  ETags + 304 short-circuit. Row caps are 50k with date pre-filter
  so the worst case is ~12-month window at current volume —
  PostgREST bandwidth usage stays linear in window size, not in
  total table size.

## Run 160 / 500 — global dashboards quartet + deploy

- **Feature**: `/api/estatisticas/resumo.json` — headline numbers
  for dashboards: lifetime total + rolling 24h/7d/30d counts +
  lifetime status split with ratios. Six parallel `head:true`
  head-counts — zero row payload, safe to poll from a TV screen.
- **Feature**: `/api/fontes/top.json` — top N source domains ranked
  by citation count in a sliding `?meses=1..120` window. Returns
  classification buckets (imprensa/oficial/video/social/outro) +
  per-type distribution — enough for a source-diversity dashboard
  in one request.
- **Feature**: `/api/estatisticas/por-semana.json` — ISO-weekly
  (UTC) status-split series for all non-removed statements,
  `?semanas=4..104`. Completes the global `{por-dia, por-semana,
  por-mes}` matrix so clients can pick the resolution they need.
- **Feature (refactor)**: `/api/estatisticas/por-mes.json` now uses
  `buildMonthlyStatusSeries` — matches the three scope routes
  refactored in Run 159. The manual `while` loop (and its
  null-date crash surface) is gone everywhere `por-mes` lives.
- **Bug fix**: `resumo.json` clamps `unverified` with `Math.max(0,
  t - v - d)` so a transient race (e.g. a row flipping status
  mid-query) can't ever emit a negative count to clients.
- **Cost opt**: `resumo.json` is six head-counts only — no row
  transfer at all. CDN-tier is aggressive (max-age=300,
  s-maxage=600, SWR=86400): polling every 5 min → 1 origin hit
  per 10 min with a 24h stale window for outages. `fontes/top.json`
  caps at 50k filtered rows (date-windowed); its weak ETag keys
  on `unique_domains` so a new domain appearing invalidates but
  reshuffled counts don't.

## Run 159 / 500 — fontes por-mes + SVGs, monthly-buckets helper, refactor sweep

- **Feature**: `/api/fontes/:domain/por-mes.json` — monthly UTC
  status-split series for a domain, `?meses=3..36`. Uses the new
  shared helper. Completes fontes' timeseries matrix (por-dia,
  por-semana, por-mes all present).
- **Feature**: `/api/fontes/:domain/sparkline.svg` — inline monthly
  sparkline for a source domain, reusing `renderSparklineSvg`.
  Drop-in for `<img>` tags on fontes pages.
- **Feature**: `/api/fontes/:domain/severity.svg` — 5-bucket
  severity histogram SVG for a source domain. Matches the existing
  partido/estado/categoria severity.svg exactly.
- **Feature (refactor)**: new `lib/stats/monthly-buckets.ts`
  (`buildMonthlyStatusSeries`, `monthlyStartStr`). Refactored
  `/api/partidos/:slug/por-mes.json`, `/api/estados/:uf/por-mes.json`,
  `/api/categorias/:slug/por-mes.json` to use it — ~40 LOC of
  duplicated gap-fill + key-splitting removed per route.
- **Bug fix**: the three refactored `por-mes.json` routes previously
  called `.slice(0,7)` on `statement_date` without a null-guard — a
  null date from a malformed row would crash the whole response.
  Helper uses optional chaining + empty-string guard so bad rows
  drop silently instead.
- **Cost opt**: three new fontes endpoints all emit content-keyed
  weak ETags + 304 handshake. Monthly series now share a single
  implementation; a future optimization (e.g. cursor or in-DB
  aggregation) lands once and benefits every scope.

## Run 158 / 500 — fontes scope quartet (severidade/verify/categorias/por-semana)

- **Feature**: `/api/fontes/:domain/severidade.json` — 5-level
  severity distribution for a source domain, five parallel head
  counts over the shared `sourceUrlOrFilter` chunk (zero rows
  transferred).
- **Feature**: `/api/fontes/:domain/verify-ratio.json` — monthly
  verify-ratio trend + summary for a domain, reusing
  `buildVerifyRatio`. Mirrors the politico/party/UF/category shape.
- **Feature**: `/api/fontes/:domain/categorias.json` — ranked
  categories appearing on statements that cite this domain, split
  into `primary` vs `total` counts. Two-step query (statement IDs
  → pivot) because PostgREST can't apply `.or()` through `!inner`.
- **Feature**: `/api/fontes/:domain/por-semana.json` — ISO weekly
  (UTC) bucket series for domain citations, reusing
  `buildWeeklyStatusSeries`. Completes the fontes scope so it now
  has the same analytics surface area as politico/partido/estado/
  categoria.
- **Bug fix**: `/api/fontes/:domain/categorias.json` uses `.in()`
  with statement IDs rather than attempting an `.or().!inner` chain
  (which silently drops domain filter in PostgREST's query
  translation). Handles the empty-match case with an early-return
  so we don't send `.in([])` which errors.
- **Cost opt**: all four routes emit weak ETags + 304 handshake.
  Severidade uses five HEAD queries (no row payload); por-semana
  caps at 10k filtered rows vs the 20k party-scope cap (fontes
  filters are narrower, no benefit from going wider).

## Run 157 / 500 — verify-ratio scope trio + shared helper

- **Feature**: `/api/partidos/:slug/verify-ratio.json`,
  `/api/estados/:uf/verify-ratio.json`,
  `/api/categorias/:slug/verify-ratio.json` — monthly verify-ratio
  trend + summary for each scope (party/UF/category). Matches the
  shape already returned by `/api/politico/:slug/verify-ratio.json`.
- **Feature**: new shared helper `lib/stats/verify-ratio.ts`
  exporting `buildVerifyRatio`, `verifyRatioStartStr`, and the
  `VerifyMonth`/`VerifySummary` types. Single source of truth for
  the UTC first-of-month anchor + gap-filled trend + ratio rounding.
- **Feature (refactor)**: `/api/politico/:slug/verify-ratio.json`
  now uses the helper — ~30 LOC of inline bucketing deleted,
  behaviour identical.
- **Bug fix**: prior inline implementation in politico/verify-ratio
  silently swallowed rows whose `statement_date` was `null` (would
  crash on `.slice`). Helper uses optional-chain + empty-string
  guard so null dates are dropped cleanly.
- **Cost opt**: all four routes emit content-keyed weak ETags
  (`{scope}-verify-ratio-{id}-{meses}-{v}-{d}-{u}`) + 304
  short-circuit, so repeat polls on unchanged windows are 0-byte.

## Run 156 / 500 — source-domain scope analytics trio

- **Feature**: `/api/fontes/:domain/stats.json` — aggregated totals
  for a source domain: total citations, verified/disputed/unverified
  split, mean severity, first/last cited dates. Single 10k-row fetch
  grouped in memory — PostgREST can't split host-like filters into
  parallel head counts.
- **Feature**: `/api/fontes/:domain/politicos.json` — ranked list of
  politicians whose statements cite this domain, with party/UF
  context. `?limite=1..50`.
- **Feature**: `/api/fontes/:domain/por-dia.json` — daily (UTC)
  status-split bucket series for one domain, reusing
  `buildDailyStatusSeries`. `?dias=7..180`.
- **Feature (refactor)**: new shared helper
  `lib/sources/domain-filter.ts` exporting `sourceUrlOrFilter(domain)`
  (the three-ilike `.or(...)` chunk) and `DOMAIN_RE_STRICT`. All four
  `/api/fontes/:domain/*` routes (stats, politicos, por-dia, jsonld)
  now use the same filter — single place to evolve the host-match
  heuristic without drift.
- **Bug fix**: `/api/fontes/:domain/jsonld` had its filter spelled
  inline — the three new routes would have diverged silently. Now
  all four share `sourceUrlOrFilter` so a future fix (e.g. handling
  subdomains) lands in one place.
- **Cost opt**: all three new routes emit content-keyed weak ETags
  with 304 short-circuit. The `por-dia` route caps rows at 10k
  (already filtered by date+status+domain), cutting worst-case
  payload vs a naïve 50k cap by 5×.

## Run 155 / 500 — politico analytics trio + 13-file SWR sweep

- **Feature**: `/api/politico/:slug/severidade.json` — per-politician
  severity histogram via five parallel head-count queries. Reuses
  `lib/severity/scoped-counts.ts` helper from Run 154 so the bucket
  shape matches the global/party/UF/category endpoints exactly.
- **Feature**: `/api/politico/:slug/por-dia.json` — daily (UTC) bucket
  series, status-split & gap-filled, `?dias=7..180` (default 60).
  Uses `buildDailyStatusSeries` so the UTC arithmetic matches the
  scope-level endpoints.
- **Feature**: `/api/politico/:slug/verify-ratio.json` — global
  verified/disputed/unverified counts + per-month trend across the
  last `?meses=3..24` months (default 12). Single row fetch grouped
  in memory; per-bucket `ratio_verified` and a summary object for
  the headline figure.
- **Bug fix**: SWR < s-maxage on 13 routes (CDN degenerate: sees a
  response as fresh forever within s-maxage, then immediately
  requires a revalidation — no actual stale window served):
  `/api/v1/politicians`, `/api/v1/politicians/:slug`,
  `/api/v1/categories`, `/api/embed`, `/api/politicians`,
  `/api/v2/statistics`, `/api/v2/statistics/by-party`,
  `/api/v2/statistics/monthly`, `/api/v2/statistics/sources`,
  `/api/v2/statistics/by-state`, `/api/v2/categories`,
  `/api/trending`, `/api/categories`. All widened to SWR=86400.
- **Cost opt**: the 13 widened SWR windows let the CDN keep serving
  last-known-good for up to a day during an origin outage / slow
  cold start, cutting both origin load and user-visible 5xx.
  Combined with the three new head-count-only endpoints (zero row
  payload for `severidade.json`), net DB bytes per probe drop by
  ~99% compared to the equivalent list-fetch.

## Run 154 / 500 — severity distribution trio + ETag on global endpoint

- **Feature**: `/api/partidos/:slug/severidade.json`,
  `/api/estados/:uf/severidade.json`,
  `/api/categorias/:slug/severidade.json` — five parallel head-count
  queries per scope, returning `{level,label,short,description,count,url}[]`
  with the canonical severity metadata from `lib/severity/labels`.
  Powers per-scope severity histograms without shipping row data.
- **Feature**: new shared helper `lib/severity/scoped-counts.ts`
  (`countBySeverity(make)`) that wraps the 5-level `Promise.all`
  and bucket-shape assembly. Global `/api/severidade.json` now reuses
  the same helper — single source of truth for the bucket schema.
- **Feature**: content-keyed weak ETag + 304 short-circuit on global
  `/api/severidade.json` (previously had no conditional response).
  Key is `sev-global-${total}-${counts.join('.')}` so any drift
  invalidates. SWR widened 3600→86400 (was below s-maxage, which is
  degenerate: CDN revalidates before it can serve stale).
- **Bug fix**: SWR < s-maxage on `/api/severidade.json`
  (3600 stale-while-revalidate vs 1800 s-maxage). Canonical tier
  applied: `max-age=900, s-maxage=1800, stale-while-revalidate=86400`.
- **Cost opt**: all four severity endpoints use `head:true` head-counts
  — zero row payload, five round-trips total vs a full scan. Global
  endpoint now short-circuits via ETag, eliminating JSON serialization
  for unchanged poll responses.

## Run 153 / 500 — scope por-dia trio + shared daily bucket helper

- **Feature**: `/api/partidos/:slug/por-dia.json`,
  `/api/estados/:uf/por-dia.json`,
  `/api/categorias/:slug/por-dia.json` — daily (UTC) statement
  bucket timeseries per scope. Status-split, gap-filled.
  `?dias=7..180` (default 60), 20k scan cap.
- **Feature** (infra): `lib/stats/daily-buckets.ts` —
  `buildDailyStatusSeries` shared with `/api/estatisticas/por-dia.json`
  (refactored to consume it). Sibling of weekly-buckets.ts so every
  UTC day-window aligns across scopes.
- **Bug fix / DRY**: `/api/estatisticas/por-dia.json` inlined its
  own bucketing + gap-fill loop duplicating logic that now lives
  in `buildDailyStatusSeries`. A stray `.slice(0, 10)` difference
  between inlined copies could subtly drift timeseries across
  endpoints. Centralizing means one chokepoint for UTC bucketing.
- **Cost opt**: three new routes ship content-keyed ETags + 304
  short-circuits + `s-maxage=1800, SWR=86400`. DB scans capped at
  20k so worst-case party/UF/category load stays bounded even as
  the tables grow.

## Run 152 / 500 — scope por-semana trio + sitemap count bug + SWR fix

- **Feature**: `/api/partidos/:slug/por-semana.json`,
  `/api/estados/:uf/por-semana.json`,
  `/api/categorias/:slug/por-semana.json` — weekly (ISO,
  UTC-Monday) statement bucket timeseries for each scope. Split by
  verification status, gap-filled via a new shared helper.
- **Feature** (infra): `lib/stats/weekly-buckets.ts` — shared
  `buildWeeklyStatusSeries` + `weekKeyFromDate` + `utcMondayOfMs`
  helpers so the three scope routes (and future ones) agree on
  what "week of YYYY-MM-DD" means. Subtle thing to inline: a stray
  timezone or DST miscount would misalign buckets across scopes.
- **Bug fix**: `/api/sitemap-statements` shipped
  `s-maxage=86400, stale-while-revalidate=3600` — SWR way shorter
  than s-maxage, so expired CDN entries hit the DB instead of
  riding SWR. Widened to `SWR=604800` (a week) so crawlers hammer
  the edge, not the DB. Sitemaps are Googlebot's favorite target.
- **Cost opt**: same route switched count: 'exact' → count:
  'estimated'. Exact ran a full aggregate on every page of every
  crawler pass — multi-second on large tables. Estimated comes
  from Postgres stats and is near-free; crawler pagination only
  needs the total accurate within a few percent.

## Run 151 / 500 — scope por-mes trio (partidos/estados/categorias) + age-decay helper

- **Feature**: `/api/partidos/:slug/por-mes.json` — monthly timeseries
  for a single party, bucketed by `statement_date` (UTC) and split
  by verification status. Gap-filled loop over month keys with
  `Date.UTC` arithmetic so every month appears. 3..36 meses.
- **Feature**: `/api/estados/:uf/por-mes.json` — same timeseries
  shape filtered by politicians.state. Validates UF as `[A-Z]{2}`.
- **Feature**: `/api/categorias/:slug/por-mes.json` — same shape
  filtered by `statement_categories.category_id` with `is_primary`
  (primary tag only) to keep signal clean.
- **Bug fix**: five ranking endpoints (`/api/ranking.json`,
  `/api/ranking.csv`, `/api/top-partidos.json`, `/api/top-estados.json`,
  `/api/ranking/por-estado.json`) all inlined
  `new Date(statement_date + 'T12:00:00Z')`. If a future schema change
  ever makes `statement_date` carry a time component, that string
  concat yields an unparseable value and the age becomes NaN →
  the row silently zero-scores and disappears from the leaderboard.
  Extracted `ageDaysFromStatementDate` + `decayScore` into
  `lib/utils/age-decay.ts` with a `.slice(0, 10)` defensive guard
  so all five callers get the fix at the chokepoint and future
  rankings inherit it.
- **Cost opt**: three new timeseries routes ship content-keyed
  ETags + 304 short-circuits; deep `s-maxage=3600` with
  `SWR=86400` means the CDN does the heavy lifting and the
  Supabase DB sees near-zero traffic on warm caches. Scan ceilings
  (30k rows) keep worst-case DB load bounded as the tables grow.

## Run 150 / 500 — estatisticas/por-dia + ranking/por-estado + politico/por-semana + more SWR fixes (DEPLOY)

- **Feature**: `/api/estatisticas/por-dia.json` — daily UTC-bucketed
  timeseries of statements by verification status over the last
  7..365 days (default 90). Gap-filled loop over UTC midnight
  timestamps so every day appears. Sibling to `/por-mes.json`.
- **Feature**: `/api/ranking/por-estado.json` — UF weighted ranking
  using `severity × exp(-ageDays/365)`. Joins `BRAZIL_TILES` so
  every UF (including zero) is present for complete choropleths.
  Returns per-UF count, weighted_score, avg_severity, rank.
- **Feature**: `/api/politico/:slug/por-semana.json` — weekly UTC-
  Monday bucket timeseries for one politician. Verified/disputed/
  total + avg_severity per bucket. 4..104 weeks (default 26),
  gap-filled. Finer grain than the existing `monthly.json`.
- **Bug fix**: four more routes with `stale-while-revalidate`
  shorter than `s-maxage` — `/api/news` (60 < 120), `/api/suggest`
  (30 < 60), `/api/politicians/:slug/statements` (60 < 300), and
  both YouTube-search cache-control strings (300 < 600). Same
  footgun as Run 149: expired CDN entries bypassed SWR and hit
  upstream. Widened SWR to 600/300/1800/3600 so stale hits ride
  the edge; YouTube especially benefits because we pay real API
  quota on every miss.
- **Cost opt**: YouTube quota savings from the SWR fix are the big
  win here (search hits with expiring cache were burning 100-unit
  quota per miss). News route rides Google News quota similarly.
  New routes also ship content-keyed ETags + 304 short-circuits.

## Run 149 / 500 — stmt oEmbed + fontes.csv + categorias/por-mes + SWR coverage fixes

- **Feature**: `/api/declaracao/:idOrSlug/oembed` — per-statement
  oEmbed 1.0 `rich` payload resolved directly by id or slug, no
  `?url=` indirection. Includes `author_name/author_url` (the
  politician), escaped iframe HTML, `cache_age`, plus a weak ETag
  keyed on `updated_at + maxWidth×maxHeight` so dimension variants
  cache independently. Friendlier to publishers with slugs in hand.
- **Feature**: `/api/fontes.csv` — CSV dump of source-domain usage
  across non-removed statements (columns: domain, count, share,
  dominant_type, url). Sibling of `/api/fontes.json`, capped
  `?limite=1..1000` (default 500). Dominant type picked by modal
  per-domain type count. ETag + 304 + `s-maxage=10800`.
- **Feature**: `/api/categorias/por-mes.json` — per-category monthly
  timeseries for the top N primary-tagged categories (verified
  only). Gap-filled across the UTC month window with `Date.UTC`
  arithmetic so clients get a dense matrix. `?meses=1..24`,
  `?top=1..30`, 50k-row scan cap.
- **Bug fix**: four endpoints shipped `stale-while-revalidate`
  shorter than `s-maxage` — `/api/v2/statements` (120 < 600?? no,
  SWR=60 < s-maxage=120), `/api/v2/politicians` (300 < 600),
  `/api/v1/statements/[id]` (300 < 600), `/api/v1/statements`
  (120 < 300). Once the fresh window closed, stale hits couldn't
  ride SWR — every expired cache entry paid a synchronous DB
  round-trip. Expanded SWR past s-maxage on all four so stale
  hits serve instantly while a background revalidation runs.
- **Cost opt**: the SWR fixes are themselves the cost opt — v2
  list endpoints are the hot path for the public API and the
  widened SWR means 5-10× more CDN hits avoid the DB round-trip.
  New CSV + oEmbed + por-mes endpoints all ship weak ETags with
  304 short-circuits so warm cache hits skip serialization.

## Run 148 / 500 — politicos por-estado/por-partido + estatisticas por-mes + /api/stats ETag

- **Feature**: `/api/politicos/por-estado.json` — active-politician
  counts aggregated by UF, joined with `BRAZIL_TILES` so every UF
  appears (including zero-count ones) with `name` + `region`. Lets
  clients render a complete Brazil map without a post-merge step.
  Capped 20k scan, weak ETag on total+count.
- **Feature**: `/api/politicos/por-partido.json` — active-politician
  counts aggregated by party, ordered desc, capped `?limite=1..200`
  (default 200). Sibling to the UF endpoint. Same ETag/CC tier.
- **Feature**: `/api/estatisticas/por-mes.json` — monthly timeseries
  of statements by `statement_date` (UTC buckets), split by
  `verified/disputed/unverified/total`. Gap-filled with `Date.UTC`
  month arithmetic. `?meses=1..48` (default 24), 50k-row ceiling.
- **Bug fix**: `/api/stats` shipped `Cache-Control: max-age=300,
  s-maxage=1800, stale-while-revalidate=300`. SWR shorter than
  s-maxage is a footgun — once the CDN entry expired, clients got
  synchronous cache misses instead of riding the stale window.
  Bumped SWR to 3600 so expired entries serve stale-then-refresh.
- **Cost opt**: `/api/stats` now ships a content-keyed weak ETag
  (`stats-${verified}-${politicians}-${categories}-${disputed}`)
  and a 304 short-circuit — the counts move slowly, so most
  conditional GETs will skip the JSON serialize and the payload.

## Run 147 / 500 — colecoes.csv + co-ocorrencia graph + contradicoes por-mes + cursor hardening

- **Feature**: `/api/colecoes.csv` — CSV directory of published
  editorial collections, paralleling `/api/colecoes.json`. Columns:
  slug, title, subtitle, published_at, statement_count, url. Caps
  scan at 500 rows with weak ETag keyed on count + `s-maxage=1800`.
- **Feature**: `/api/indices/co-ocorrencia.json` — category
  co-occurrence graph. Scans up to 50k `statement_categories` rows
  from verified statements, groups tags per statement, and emits
  the top co-occurring category pairs (ordered key a<b to dedupe).
  Params: `limit=1..500`, `min_count` (default 2). Ships with an
  ETag keyed on statements-scanned + pair-count, and `s-maxage=7200`
  since the graph mutates slowly.
- **Feature**: `/api/contradicoes/por-mes.json` — monthly timeseries
  of published contradiction pairs. Fixed-window UTC buckets
  (`getUTCFullYear/Month/Date.UTC`), gap-filled across the requested
  range so clients plot without post-processing. `?meses=1..36`
  (default 12), 20k-row ceiling, ETag on total + rows scanned.
- **Bug fix**: `decodeCursor` in `lib/utils/cursor-pagination.ts`
  accepted arbitrary strings in `v` / `id` and every callsite
  spliced those straight into `.or(statement_date.gt.${v},and(...,id.gt.${id}))`
  filters. A tampered base64 payload containing `,()\:` could break
  grammar and exfiltrate rows. Added a PostgREST-grammar reject
  regex (`[,()\\:]`) to `decodeCursor` so the guard lives at the
  one chokepoint — every cursor route in `app/api/**` inherits it.
- **Cost opt**: new routes ship weak ETags + 304 short-circuits so
  warm CDN hits skip the JSON serialize and downstream byte cost.
  `co-ocorrencia` caps scan at 50k rows; `por-mes` caps at 20k —
  even as the tables grow, worst-case DB load stays bounded.

## Run 131 / 500 — scope×declaracoes cursor feeds + recent.json has_more fix

- **Feature**: `/api/politico/:slug/declaracoes.json`,
  `/api/partidos/:slug/declaracoes.json`,
  `/api/estados/:uf/declaracoes.json`,
  `/api/categorias/:slug/declaracoes.json` — four new
  cursor-paginated list endpoints so each scope now has a
  uniform way to walk every statement in it. Accept
  `limit/cursor/status/order` (and `primary=1` for categorias).
  Uses the shared `decodeCursor/buildCursorPagination` utils
  with `statement_date + id` keyset pagination, so deep
  iteration stays O(limit) regardless of total size.
- **Bug fix**: `/api/recent.json` — `has_more: rows.length === lim`
  was a classic overfetch-off-by-one: when the final page
  happened to land on exactly `lim` rows, the endpoint
  advertised a non-existent next page and clients would fetch
  an empty page. Switched to `range(offset, offset + lim)`
  (overfetch by 1) + slice so `has_more` reflects reality.
- **Cost opt**: the four new declaracoes endpoints ship
  `max-age=300, s-maxage=900, stale-while-revalidate=3600`.
  Browser reload inside 5 min is free; a single CDN hit
  warms every downstream reader for 15 min. Deep paginators
  ride the keyset cursor — no OFFSET scan regardless of page.

## Run 132 / 500 — top-level declaracoes + similar + scope leaderboards

- **Feature**: `/api/declaracoes.json` — thin top-level cursor-
  paginated list of statements. Same keyset semantics as the
  scope variants but no scope filter, plus optional `from/to/
  min_severity/status/order`. Fills the "just give me every
  statement, newest first" slot without forcing clients to
  learn the full `/api/v2/statements` surface.
- **Feature**: `/api/declaracao/[id]/similar.json` — similar-
  statements endpoint using a shared-category Jaccard-ish score
  (primary tags double-weighted), with bonuses for same
  politician / party / state. Resolves the target by UUID or
  slug. Mirrors the politician-level `similar.json` so both
  entity types expose "more like this".
- **Feature**: `/api/top-partidos.json` and
  `/api/top-estados.json` — leaderboards over parties / UFs
  using the same `severity × exp(-age/365)` decay as
  `/api/ranking.json`, with avg severity surfaced alongside.
  Ready for dashboard cards or national-overview pages.
- **Bug fix**: `/api/politico/[slug]/sparkline.json` divided
  `severity_sum / count` to produce `avg_severity`, but statements
  with `severity_score = null` still bumped `count`, silently
  dragging the average toward zero. Tracked severity_n separately
  so null scores no longer dilute the metric.
- **Cost opt**: two new leaderboards ship content-keyed weak
  ETags; declaracoes feed rides the same cursor utility so deep
  pagination stays O(limit). Similar.json caches at
  s-maxage=3600 + SWR=86400 — one DB hit per statement per
  hour across the CDN edge.

## Run 133 / 500 — politico reactions + monthly + v2 contradictions

- **Feature**: `/api/politico/:slug/reactions.json` — aggregate
  public reaction totals (importante / contestada /
  fora_de_contexto / erro) across all non-removed statements by
  one politician, plus a top-N list of the most-reacted
  statements so moderators can spot lightning rods. Chunks the
  `IN()` against `statement_reaction_counts` at 500 ids so deep
  catalogs stay under PostgREST URL limits.
- **Feature**: `/api/politico/:slug/monthly.json` — dense
  monthly timeseries (count, verified, disputed, unverified,
  avg severity with proper null-n denominator, cumulative
  total) over a configurable 3–60 month window. Richer than
  `sparkline.json`; suitable for dashboards and CSVs.
- **Feature**: `/api/v2/contradictions` — cursor-paginated
  contradiction list with both statements + politician
  inlined, plus `?politico=<slug>` and `?min_severity`
  filters. First v2 endpoint for contradictions, mirroring
  the v2/statements shape.
- **Bug fix**: `lib/analytics/aggregate-statements.ts` built
  monthly bucket keys with `new Date(y, m, 1)` (local time)
  then read them back with `getUTCFullYear/getUTCMonth`. The
  key string was sometimes off by one month relative to the
  UTC `statement_date` column for the last few hours of
  each local month, so the shared `/partidos/:slug/analise`,
  `/estados/:uf/analise`, and `/categorias/:slug/analise`
  pages occasionally missed or double-counted a month's
  statements. Switched to `Date.UTC(...)`.
- **Cost opt**: all three new endpoints emit weak content-
  keyed ETags. Reactions and monthly use
  `s-maxage=900–3600 + SWR=3600–86400`; v2/contradictions
  uses `s-maxage=300 + SWR=900`. Chunked IN() on reactions
  prevents URL-length blowup on high-statement-count
  politicians.

## Run 134/500 — 2026-04-19

- Feature: `/api/v2/politicians/:slug` detail endpoint with
  field selection (`?fields=`) and opt-in includes
  (`?include=statement_count,categories`). Complements the
  v2 list endpoint; edge runtime; s-maxage=600 + SWR=1800.
- Feature: `/api/declaracoes.csv` bulk CSV mirror of the
  top-level `declaracoes.json` feed. Up to 5000 rows per
  request with shared `csvEscape` (formula-injection safe).
  Filters: limit/status/from/to/min_severity/order.
- Feature: `/feed.atom` global Atom 1.0 feed, sibling to
  `/feed.xml` RSS. Stricter id/updated semantics for
  aggregators that prefer Atom (Feedly, Inoreader).
- Bug fix: `/api/v2/politicians` search sanitization.
  Previously escaped only LIKE metachars (`%_`) but left
  PostgREST or-grammar metachars (`,()` + backslash)
  unfiltered, so a user-supplied `q` like `a,b)` could
  break out of the ilike clause. Same family as Run 129's
  v2/statements fix — now applied consistently.
- Cost opt: Atom feed reuses `getRecentStatements(30)` so
  an aggregator hitting both /feed.xml and /feed.atom pays
  one query pattern. CSV endpoint caps at 5000 rows and
  uses s-maxage=900 + SWR=3600 so repeated bulk-pulls hit
  the edge cache instead of Postgres.

## Run 135/500 — 2026-04-19

- Feature: `/api/v2/statements/:idOrSlug` detail endpoint.
  Accepts UUID or slug, supports `?fields=` and
  `?include=politician,categories`, emits an updated_at-keyed
  weak ETag for polling aggregators. Edge runtime.
- Feature: `/api/politico/:slug/contradictions.csv` — CSV
  export of every published contradiction pair for a
  politician with both statements inlined side-by-side
  (research-friendly flat schema, shared `csvEscape`).
- Feature: `/api/embed/politico/:slug` — standalone HTML
  card (photo + party/state + verified count) sized for
  `<iframe width=360 height=180>`. Inline CSS so cross-site
  embeds don't hit CSP; `X-Frame-Options: ALLOWALL` and
  `Content-Security-Policy: frame-ancestors *` let anyone
  embed. Edge runtime with head-count query (no row read).
- Bug fix: `/api/v2/statements` previously passed the raw
  `?status=` value into `eq('verification_status', status)`.
  `status=removed` would leak soft-deleted rows; any other
  non-whitelisted value (e.g. `all`) silently returned an
  empty page instead of signaling the bad input. Now
  whitelisted to `verified|disputed|unverified` with
  fallback to `verified`.
- Cost opt: Embed endpoint uses `select('id', { count:
  'exact', head: true })` so Postgres returns only the
  count header, not the row payload. Contradictions CSV
  reuses the shared `listContradictionsForPolitician`
  helper — no second SELECT pattern to keep in sync.

## Run 136/500 — 2026-04-19

- Feature: `/api/v2/contradictions/:id` — v2 detail endpoint
  for a single contradiction pair. Complements the v2 list
  at `/api/v2/contradictions`. Flat shape (politician URL +
  both statements with permalinks), ETag on updated_at,
  strict UUID regex at the boundary, `X-API-Version: v2`.
- Feature: `/api/embed/declaracao/:idOrSlug` — standalone
  HTML card for a single statement (quote + author +
  status pill + date + source link). Sized for
  `<iframe width=420 height=240>`, inline CSS, ALLOWALL
  frame headers. Accepts UUID or slug.
- Feature: `/api/politicos.csv` — bulk CSV sibling to
  `/api/politicos.json`. Up to 5000 rows with optional
  `?partido` and `?estado` filters, `?ativo=false` to
  include inactive. Shared `csvEscape`.
- Bug fix: `/api/contradicoes/[id].json` accepted any
  8–36-char hex-or-dash string via `/^[0-9a-f-]{8,36}$/`.
  That let malformed ids like `--------` reach Postgres
  for a guaranteed 404 — wasted round-trip. Tightened to
  strict UUID regex so bad input 400s at the edge.
- Cost opt: Embed declaration card uses a single joined
  query (politicians inner) instead of two hops. Politicos
  CSV caps rows, filters at Postgres level, and serves
  with `s-maxage=3600 + SWR=86400` so daily-crawling
  datasets hit the edge cache.

## Run 137/500 — 2026-04-19

- Feature: `/api/contradicoes.csv` — bulk CSV of every
  published contradiction pair. One row per pair with both
  statements inlined side-by-side (pair_id + politician +
  low/high date/summary/source/url), capped at 2000.
- Feature: Taxonomy CSVs — `/api/partidos.csv` (parties with
  active politician counts) + `/api/categorias.csv` (full
  category directory including severity tier + description).
  Siblings to the existing .json variants, using shared
  `csvEscape` for formula-injection safety.
- Feature: `/politico/:slug/feed.atom` — per-politician
  Atom 1.0 feed. Sibling to the existing per-politician
  `feed.xml` (RSS 2.0) for aggregators that prefer Atom
  (Feedly, Inoreader, NetNewsWire). 50 latest non-removed
  statements.
- Bug fix: `/api/partidos.json` fetched `politicians.party`
  with no `.limit(...)`, so a large `politicians` table
  (e.g. after a bulk import of historical candidates) would
  stream tens of thousands of single-column rows through
  the edge runtime just to tally. Capped at 20k.
- Cost opt: All three new CSV routes serve with
  `s-maxage=1800–86400 + SWR=3600–86400` so daily crawls
  hit the edge cache. Per-politician Atom reuses the same
  top-50 query shape as feed.xml (Supabase can plan either
  off the same statement_date index).

## Run 138/500 — 2026-04-19

- Feature: `/api/estados.csv` — CSV directory of the 27
  UFs with active-politician and verified-statement counts.
  Completes the taxonomy CSV set (politicos + partidos +
  categorias + estados all have .csv siblings now).
- Feature: Scope Atom feeds — `/partidos/:slug/feed.atom`,
  `/estados/:uf/feed.atom`, `/categorias/:slug/feed.atom`.
  Sibling to the existing per-scope RSS feeds for
  aggregators that prefer Atom. All three use the new
  shared `lib/feeds/atom.ts` renderer so the output shape
  stays consistent across scopes (one source of truth for
  entry templating + headers).
- Feature: `/api/embed/contradicao/:id` — standalone HTML
  card for a contradiction pair. Headline + politician +
  both statements side-by-side with dates, sized for
  `<iframe width=520 height=280>`, CSP frame-ancestors *
  to allow cross-site embeds.
- Bug fix: `/partidos/[slug]/feed.xml` fetched ALL active
  politician IDs of the party (no limit), then passed
  them as `.in('politician_id', ids)`. For large parties
  this pushed the PostgREST request URL past nginx's
  default 8KB limit. Rewrote as a single
  `politicians!inner` join filter — one round trip, no
  URL blowup, and lets Postgres pick the right index.
- Cost opt: Shared Atom renderer + single join for the
  party feed both reduce round trips. Estados CSV reuses
  the same dual-query pattern as estados.json (one
  query-planner path, same cache key family). All three
  scope Atom feeds edge-run with `s-maxage=3600 + SWR=600`.

## Run 139/500 — 2026-04-19

- Feature: `/api/v2/categories/:slug` — v2 detail endpoint
  for editorial categories. Complements the v2 list.
  `?include=statement_count` (primary-tagged, non-removed
  count via head-only count query) and
  `?include=children` (child categories by parent_id).
  Edge runtime, `s-maxage=3600 + SWR=86400`.
- Feature: `/api/ranking.csv` — CSV sibling to the weighted
  politician ranking. Same severity × exp(-age/365) math
  as `ranking.json`, up to 200 rows (vs 100 on JSON) since
  CSV consumers (Google Sheets, Observable, pandas) tend
  to want deeper tables.
- Feature: `/api/badge/politico/:slug` — shields.io-style
  SVG badge showing verified statement count. Pairs with
  any README / dashboard via `<img src=...>`. Accepts
  `?color=#hex` and `?label=` overrides. Uses a small
  built-in text-width heuristic so we stay edge-runtime
  (no canvas, no metrics table).
- Feature: Enhanced `/api/oembed` with `/contradicoes/:id`
  support — Slack/Discord/Mastodon unfurls of contradiction
  URLs now auto-render as embedded cards via
  `/api/embed/contradicao/:id` (Run 138's HTML route).
- Bug fix: `/api/estados.json` first query
  (`politicians.state` scan) had no `.limit(...)` — same
  family as the partidos.json fix. Capped at 20k.
- Cost opt: Badge uses `count: 'exact', head: true` so
  Postgres returns only the count header. Ranking CSV
  reuses the JSON's 5000-row pull pattern — if the JSON
  is cached at s-maxage=1800, the CSV warms the same
  planner path.

## Run 140/500 — 2026-04-19 (deploy batch 14)

- Feature: `/api/badge/partido/:slug` — shields.io-style
  badge for parties. `?metric=declaracoes` (default) uses
  a `politicians!inner` join head-count; `?metric=politicos`
  counts active politicians directly.
- Feature: `/api/badge/estado/:uf` — shields.io-style badge
  for UFs, same declaracoes/politicos metric toggle.
- Feature: `/api/fact-checks.csv` — CSV sibling of the
  fact-checks feed. Flattens the nested statement +
  politician relation, respects the `?rating=` filter,
  up to 2000 rows.
- Refactor: Moved badge SVG renderer to shared
  `lib/badges/shields.ts` so new `/api/badge/*` scopes
  share one text-width heuristic + color + header pattern
  (politico badge now imports from there too).
- Bug fix: `/api/oembed` rejected canonical URLs when the
  configured `SITE_URL` carried a `www.` prefix — it would
  compute `www.www.host` as the acceptable alias and
  mismatch the naked `host`. Normalized both sides by
  stripping `www.` and lowercasing.
- Cost opt: All three badges use
  `count: 'exact', head: true` so Postgres returns only
  the count header. Fact-checks CSV reuses the JSON's
  query shape + filter semantics so the planner hits the
  same index family.

## Run 141/500 — 2026-04-19

- Feature: `/api/contradicoes/por-partido.json` — leaderboard
  aggregating published contradiction pairs by politician party.
  ETag keyed on scan size + bucket count so mirrors pay 304s.
- Feature: `/api/contradicoes/por-estado.json` — same leaderboard
  by UF, capped at 27 rows.
- Feature: `/api/recent.jsonfeed` — JSON Feed 1.1 variant of the
  recent-statements feed. Sits alongside `/feed.xml` (RSS) and
  `/feed.atom` for readers that prefer the spec.
- Feature: `/api/declaracao/:idOrSlug/jsonld` — schema.org
  ClaimReview JSON-LD for one declaration. Lets aggregators
  ingest structured claim data without scraping HTML.
- Bug fix: `listContradictionsMentioningStatement` in
  `lib/contradictions/queries.ts` interpolated the statement id
  straight into a PostgREST `.or(...)` without a UUID gate and had
  no `.limit(...)` — unbounded result set + injection surface. Now
  rejects non-UUID input early and caps at 50 rows.
- Cost opt: All four new endpoints ship weak ETags +
  `Cache-Control: s-maxage=...` so repeat polls pay 304s. JSON-LD
  uses the directory tier (`s-maxage=3600, SWR=86400`) since per-
  statement claim data rarely changes after publication.

## Run 142/500 — 2026-04-19

- Feature: `/api/agenda.ics` — iCal (RFC 5545) feed of
  political events. Subscribers see curated agenda in their
  calendar app. Fold/esc helpers + UTC DTSTAMP + ETag keyed on
  newest updated_at.
- Feature: `/api/politico/:slug/jsonld` — schema.org Person +
  ProfilePage JSON-LD graph so Google/Bing can connect the
  politician page to structured data (affiliation, role,
  birthDate, homeLocation).
- Feature: `/api/verify-ratio.json` — monthly timeseries of
  verified/disputed/unverified counts over the last N months
  (max 36). Dashboards can chart editorial throughput without
  re-aggregating the dump.
- Bug fix: `/api/v1/search` sanitizer stripped only `,().` but
  left `\`, `:`, `%`, `_` — opening filter injection and
  wildcard scans on user input. Now matches the canonical
  `[,()\\:]` + `[%_]` pattern. Returns an empty result when the
  query reduces to whitespace.
- Bug fix: `/api/v2/categories` q-param escaped only LIKE
  metachars (`%_`) and left PostgREST or-grammar chars intact.
  Same canonical strip applied.
- Cost opt: All three new endpoints ship weak ETags. iCal feed
  uses `s-maxage=900 + SWR=3600` so calendar clients pay 304s
  between their typical hourly polls. JSON-LD uses the
  directory tier (`s-maxage=3600 + SWR=86400`).

## Run 143/500 — 2026-04-19

- Feature: `/sitemap-news.xml` — Google News sitemap with
  verified statements from the last 48h. Uses the news
  sitemap namespace + publication_date so News bot doesn't
  have to swim through 2000+ general URLs.
- Feature: `/api/partidos/:slug/jsonld` — schema.org
  Organization + CollectionPage graph for a party. Head-only
  counts (active politicians + verified statements) via
  `politicians!inner` join.
- Feature: `/api/categorias/:slug/jsonld` — schema.org
  DefinedTerm + CollectionPage graph for a category. Links
  to a shared DefinedTermSet, head-counts primary statements.
- Bug fix: `app/sitemap.ts` had four unbounded scans
  (politicians, parties, states, categories, collections) —
  a growth surprise would OOM the build. Capped all five at
  20k / 5k sensibly.
- Cost opt: Both JSON-LD endpoints use parallel head-only
  counts + directory cache tier (`s-maxage=3600 + SWR=86400`).
  News sitemap pulls 1000 rows in one query; ETag keyed on
  newest created_at so News bot polls pay 304s between
  editorial drops.

## Run 144/500 — 2026-04-20

- Feature: `/api/colecoes/:slug.json` — machine-readable
  detail for a published editorial collection (up to 500
  curated items in their authored position order). Fills
  the gap left by `/colecao/:slug/feed.{json,xml}` which
  emit feed shapes instead of a plain detail object.
- Feature: `/api/partidos/:slug/leaderboard.json` — top
  politicians in a party scored by severity × exp(-ageDays
  /365). Party landing pages can surface "who's driving the
  scorecard" without a second query.
- Feature: `/api/verify-ratio/por-partido.json` — party-
  level verified/disputed/unverified counts over N months
  so dashboards can compare editorial coverage across
  parties.
- Bug fix: `lib/monthly-recap/compute.ts#currentMonth` used
  local-time `getFullYear/getMonth` — late-night-BRT
  registrations bucketed into "next month" that never
  matched the UTC DB. Switched to `getUTCFullYear/UTCMonth`.
- Bug fix: `lib/utils/chart-data.ts#bucketDate` also used
  local-time methods on a parsed YYYY-MM-DD string, so
  every chart with month/week aggregation drifted one bucket
  for BRT rows near the boundary. All buckets now UTC; week
  bucket now computes Sunday UTC via `Date.UTC`.
- Cost opt: Leaderboard reuses the `politicians!inner`
  filter pattern (one query, no two-step `.in()`), capped at
  20k. All three endpoints ship weak ETags + tiered cache.

## Run 145/500 — 2026-04-20

- Feature: `/api/estados/:uf/jsonld` — schema.org
  AdministrativeArea + CollectionPage graph. Completes the
  scope JSON-LD set (politico / partido / categoria /
  estado). Head-counts active politicians and verified
  statements via `politicians!inner` join.
- Feature: `/api/estados/:uf/leaderboard.json` — top
  politicians in the UF scored by severity × exp(-ageDays
  /365). Sibling of the partidos leaderboard.
- Feature: `/api/verify-ratio/por-estado.json` — per-UF
  verify ratios over N months. Sibling of
  /verify-ratio/por-partido.json.
- Bug fix: `lib/supabase/queries/statistics.ts` had two
  month-key loops with local-time `getFullYear/getMonth`
  (statement_date is UTC YYYY-MM-DD) — final bucket drifted
  on BRT near midnight. Also `getPartyDistribution` scanned
  the whole verified-statements table unbounded; capped at
  50k.
- Bug fix: `lib/supabase/queries/compare.ts` monthly fill
  had the same local-time loop; now UTC.
- Cost opt: Estado JSON-LD uses parallel head-only counts.
  Leaderboard uses `politicians!inner` (one query, no
  two-step `.in()`). All three endpoints ship weak ETags +
  tiered cache (directory for JSON-LD, aggregate for
  leaderboard and verify-ratio).

## Run 146/500 — 2026-04-20

- Feature: `/api/categorias/:slug/leaderboard.json` — top
  politicians in a category scored by severity × exp(-ageDays
  /365). Completes the scope leaderboard trio (partidos,
  estados, categorias). Pulls via the statement_categories
  pivot with `is_primary = true` + `verification_status =
  verified` + `politicians.is_active = true` in a single
  PostgREST join, capped at 30k.
- Feature: `/api/fontes/:domain/jsonld` — schema.org JSON-LD
  for a source domain. Maps our internal classification
  (oficial / imprensa / video / social / outro) to schema.org
  GovernmentOrganization / NewsMediaOrganization / WebSite +
  a CollectionPage that surfaces verified-statement count.
- Feature: Parallelized `getWeeklyCounts` in
  `lib/supabase/queries/dashboard.ts`. Was a sequential for-
  loop of 12 head-counts — 12 round-trips every dashboard
  load. Now Promise.all so the admin dashboard pays ~1 RTT
  instead of 12.
- Bug fix: `lib/supabase/queries/stats.ts` monthly series
  used local-time `getMonth/getFullYear` against UTC
  statement_date. Final bucket drifted on BRT past midnight;
  now UTC.
- Bug fix: Re-gate fontes JSON-LD domain **after**
  decodeURIComponent — the pre-decode regex allowed `%` (for
  URL encoding), but `%` is a LIKE wildcard once decoded.
  Post-decode a strict `[a-z0-9.-]` gate blocks it before
  the `.or(...)` interpolation.
- Cost opt: Weekly-counts parallelization cuts admin
  dashboard blocking time. JSON-LD uses head-only counts.
  All three new endpoints ship weak ETags + tiered cache.
